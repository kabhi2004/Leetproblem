class Solution {
    public boolean uniformArray(int[] nums1) {
        
        // For n >= 2, it is always possible to construct
        // an array where all elements have the same parity.
        return true;
    }
}